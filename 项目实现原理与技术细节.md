# 当前项目实现原理与技术细节

## 1. 项目本质

这个项目本质上不是“调用 OpenAI API 批量翻译 Excel”，而是一个基于本地浏览器自动化的工作流工具：

1. 用 Node.js 启动一个本地控制台页面。
2. 用 Playwright 接管已经登录 ChatGPT 的 Chrome 页面。
3. 从 Excel 中按批次读取一列文本。
4. 把这一批文本一次性发送给 ChatGPT。
5. 等待回复稳定后，把返回结果解析成二维表格。
6. 再用 Python `openpyxl` 把结果写回 Excel。

所以它的核心思路是：

- 不直接调用模型 API。
- 不直接控制 WPS 界面完成复制粘贴。
- 而是“浏览器自动化 + 文件级 Excel 读写”。

这也是为什么仓库虽然名字里有 `wps`，但当前主流程其实主要依赖的是 ChatGPT 网页和 `openpyxl`。

## 2. 架构拆分

当前工程可以理解成 4 个层次。

### 2.1 启动层

- `package.json`
- `scripts/app-launcher.mjs`

作用：

- `npm start` 会运行 `scripts/app-launcher.mjs`。
- 这个脚本先拉起本地 UI 服务。
- 然后轮询 `http://127.0.0.1:4312/api/status`，确认服务就绪。
- 如果是在 macOS，会自动用默认浏览器打开控制面板页面。

这一层只负责“把系统启动起来”，不负责业务循环。

### 2.2 控制台与配置层

- `scripts/ui-server.mjs`
- `ui/index.html`
- `automation.config.json`

作用：

- `ui-server.mjs` 提供一个非常轻量的 HTTP 服务，没有使用 Express 之类框架。
- `ui/index.html` 是纯静态页面，直接通过 `fetch` 调后端接口。
- 所有用户配置都落在根目录的 `automation.config.json`。

UI 做的事情主要有：

- 选择 Excel 文件。
- 读取 sheet 列表。
- 修改批次大小、起始行、源列、目标列等参数。
- 启动或停止任务。
- 每 2 秒轮询状态和日志。

后端做的事情主要有：

- 读取和归一化配置。
- 选择文件时调用 Python 获取工作簿信息。
- 启动真正的执行进程 `scripts/chatgpt-wps-loop.mjs`。
- 维护运行状态、PID、退出码和日志缓存。

### 2.3 浏览器执行层

- `scripts/chatgpt-wps-loop.mjs`
- Playwright
- Chrome DevTools Protocol

作用：

- 连接到带远程调试端口的 Chrome。
- 锁定 ChatGPT project 页面或聊天页。
- 把 Excel 批次内容发送给 ChatGPT。
- 等待回答稳定。
- 把回答转换成可写回 Excel 的二维矩阵。

这一层是整个业务闭环的调度中心。

### 2.4 Excel 适配层

- `scripts/workbook_helper.py`
- `openpyxl`

作用：

- 提供工作簿元信息查询。
- 负责批量读取单元格。
- 负责批量写回单元格。
- 用 Python 把 Node 和 Excel 文件格式隔开。

这一层相当于项目里的“Excel 读写驱动”。

## 3. 主执行链路

主流程在 [scripts/chatgpt-wps-loop.mjs](/Users/zhiyangcui/Documents/vibe localization/scripts/chatgpt-wps-loop.mjs)。

它的执行可以分成两个阶段。

### 3.1 bootstrap 阶段

命令：

```bash
npm run bootstrap
```

目标不是执行 Excel 任务，而是完成 ChatGPT 项目绑定。

实现逻辑：

1. 加载 `automation.config.json`，如果不存在就创建默认配置。
2. 根据配置连接 Chrome。
3. 要求用户手动把 ChatGPT 打开到目标 project 或 project 内某个聊天页。
4. 用户回到终端按一次 Enter。
5. 脚本读取当前页面 URL。
6. 通过 `deriveProjectUrl()` 从当前 URL 反推出项目主页 URL。
7. 把 `chatgpt.targetUrl` 和 `chatgpt.projectUrl` 保存到配置文件。

这里的关键点是：项目并没有存“提示词模板”，而是把“你当前所在的 ChatGPT project 页面”当成上下文容器。后面每次新开对话，都是回到这个 project 下继续新建聊天。

### 3.2 正式运行阶段

命令：

```bash
npm start
```

点击页面里的“开始任务”后，会由 UI 服务再启动一次：

```bash
node scripts/chatgpt-wps-loop.mjs
```

然后进入 `runLoop()` 主循环。

## 4. runLoop 的工作原理

`runLoop()` 是项目最核心的函数，它控制整条批处理流水线。

每一轮循环大致如下：

1. 检查是否达到 `maxLoops` 上限。
2. 检查当前对话是否已经跑满 `resetConversationEveryRuns`。
3. 如果跑满了，就进入同一个 ChatGPT project，新建一个 fresh chat。
4. 从 Excel 的源列中读取从 `startRow` 开始的一批数据。
5. 如果整批内容全空，并且 `stopWhenEntireBatchEmpty=true`，则停止。
6. 把这一批文本用换行符拼成一个大 prompt。
7. 把 prompt 发给 ChatGPT。
8. 轮询等待回复生成完毕并稳定。
9. 把回复解析成二维矩阵。
10. 从目标列起始位置把矩阵写回 Excel。
11. 行号向下推进 `batchSize`，进入下一轮。

这说明当前项目采用的是“整批发送、整批回写”的设计，不是一行一问一答。

## 5. 浏览器自动化原理

### 5.1 为什么能接管 ChatGPT 页面

项目用的是 Playwright，但不是默认新开一个完全隔离的无状态浏览器，而是优先采用 `attach` 模式：

- 检查本地 Chrome 的远程调试端口是否可访问。
- 如果没开，在 macOS 下自动用 `open -na "Google Chrome"` 拉起一个带 `--remote-debugging-port` 的 Chrome。
- 然后通过 `chromium.connectOverCDP()` 接入这个 Chrome。

这套做法的意义是：

- 可以复用你已经登录过 ChatGPT 的浏览器身份。
- 不用自己处理登录态、验证码、人机校验。
- 可以直接操作真实的 ChatGPT Web 页面。

所以这个项目的核心前提之一是：浏览器里要先能正常访问和使用 ChatGPT。

### 5.2 页面选择策略

脚本会在当前 Chrome 的所有页面里寻找 URL 以 `https://chatgpt.com/` 开头的页面，优先拿最后一个匹配页作为目标页。

如果找不到现成页面，才会尝试新建页面并打开 ChatGPT 首页。

这意味着：

- 如果你同时开了很多 ChatGPT 标签页，脚本可能依赖“最近打开的那个”。
- bootstrap 时文档要求把目标页面变成最近打开或唯一的 ChatGPT 页，这不是随便写的，而是跟页面选择实现直接相关。

### 5.3 新对话策略

为了避免一个会话无限累积上下文，项目引入了两个控制参数：

- `resetConversationEveryRuns`
- `newConversationLimit`

逻辑是：

- 每个会话最多处理固定轮数。
- 达到上限后，回到 project 页面尝试点击 “New chat / 新对话”。
- 一共最多再新开若干个会话。
- 超出上限后停止。

这个设计本质上是在控制上下文膨胀、页面性能下降和回复漂移风险。

## 6. Excel 读写原理

Excel 相关逻辑在 [scripts/workbook_helper.py](/Users/zhiyangcui/Documents/vibe localization/scripts/workbook_helper.py)。

Node 本身不直接解析工作簿，而是把这部分工作外包给 Python：

- `info`：返回 sheet 名称和当前活动 sheet。
- `read-batch`：按列和行区间读取一批单元格。
- `write-batch`：把二维矩阵写回某一列起点。

### 6.1 为什么用 Python

因为 `openpyxl` 对 `.xlsx` / `.xlsm` 这类工作簿的支持比较成熟，尤其适合：

- 按单元格精确读写。
- 保留 `.xlsm` 宏工作簿结构。
- 做简单稳定的批处理。

### 6.2 读取策略

读取时只关心：

- 文件路径
- sheet 名
- 源列
- 起始行
- 批次大小

每个单元格都会被转成字符串，空值转成空字符串。这样可以保证传回 Node 侧时类型简单统一。

### 6.3 回写策略

回写时传入的是一个二维矩阵 `matrix`。

比如：

```json
[
  ["结果1"],
  ["结果2"],
  ["结果3"]
]
```

或者：

```json
[
  ["译文", "说明"],
  ["译文2", "说明2"]
]
```

Python 侧会把目标列字母换算成列号，然后按“行偏移 + 列偏移”逐格写入。

### 6.4 原子写入思路

`write_batch()` 不是直接覆盖原文件，而是：

1. 先在同目录创建临时文件。
2. 把工作簿保存到临时文件。
3. 再用 `replace()` 替换目标文件。

这是一种比较标准的“原子替换”思路，可以降低写到一半文件损坏的概率。

不过它仍然不能解决“另一个程序正在占用文件”的问题，所以 README 和 UI 都提醒不要同时用 WPS 打开同一个文件。

## 7. Prompt 与响应解析原理

### 7.1 Prompt 构造方式

当前项目没有复杂的 prompt 模板引擎。

它只是把一批单元格内容按换行拼起来：

```text
value1
value2
value3
...
```

也就是说，真正决定输出格式的，通常不是这段脚本本身，而是你在 ChatGPT project 里预先设定的指令、上下文和输出规范。

### 7.2 回复完成的判定方式

项目不会只靠“出现了一条 assistant 消息”就认为结束，而是组合了两层判断：

1. 页面中 assistant 消息数量比发送前增加了。
2. 最新 assistant 消息内容在连续数次轮询中保持不变，并且页面上已经没有 “Stop / 停止” 按钮。

这是一种比较实用的“稳定收敛判定”：

- 能避免刚出现消息时就过早抓取。
- 能降低抓到半截回答的概率。

如果超时，会在 `debug/` 下保存截图，方便排查页面状态。

### 7.3 回复格式支持

`parseAssistantResponseToMatrix()` 支持两种主要格式。

第一种，JSON：

- 顶层直接是数组。
- 或者对象里有 `results` 数组。

第二种，纯文本块：

- 按换行分行。
- 每一行再按 `\t` 制表符拆列。

所以最理想的 ChatGPT 输出其实是：

- JSON 数组，适合结构化回写。
- 或 TSV 文本，适合直接映射成二维单元格区域。

### 7.4 Code fence 清洗

项目会优先处理 Markdown code block。

例如 ChatGPT 返回：

````markdown
```json
["a", "b"]
```
````

脚本会先提取围栏里的内容，再尝试 JSON 解析。

另外它还会去掉某些常见的“语言标签首行”，例如：

- `json`
- `markdown`
- `text`
- `csv`
- `tsv`

这个细节说明作者考虑过 ChatGPT 经常会把有效内容包在代码块里的情况。

## 8. UI 和后端接口如何协同

控制面板页面 [ui/index.html](/Users/zhiyangcui/Documents/vibe localization/ui/index.html) 是纯原生 HTML + CSS + JS，没有使用 React/Vue。

接口也很简单：

- `GET /api/config`：读取配置、状态、派生统计和工作簿信息。
- `POST /api/config`：保存 workbook/workflow 配置。
- `POST /api/choose-file`：调用 macOS 文件选择器选择工作簿。
- `POST /api/run`：启动后台任务。
- `POST /api/stop`：停止任务。
- `GET /api/status`：读取状态和日志。

其中有两个实现细节值得注意。

### 8.1 派生统计不是实际进度

页面里的：

- 总对话窗口数
- 总循环次数
- 理论总行数

都是通过 `buildDerivedInfo()` 根据配置推导出来的理论值，不是执行后的真实统计值。

例如：

- 总对话窗口数 = `newConversationLimit + 1`
- 总循环次数 = `总对话窗口数 * resetConversationEveryRuns`
- 理论总行数 = `总循环次数 * batchSize`

这是“任务容量预估”，不是“实时完成进度”。

### 8.2 日志是内存态缓存

UI 服务会把子进程输出按行塞进 `status.logLines`，只保留最近 300 行。

所以：

- 页面看到的是轻量实时日志。
- 不是持久化审计日志。
- 重启 UI 进程后，内存日志会丢失。

## 9. 平台依赖与运行前提

这个项目虽然大部分逻辑跨平台，但当前完整体验明显偏向 macOS。

原因有几条：

### 9.1 文件选择依赖 `osascript`

`/api/choose-file` 通过 AppleScript 弹系统文件选择框：

- 这基本是 macOS 专属能力。

### 9.2 自动打开 UI 依赖 `open`

`app-launcher.mjs` 在 macOS 下用 `open http://127.0.0.1:4312` 自动拉起浏览器。

### 9.3 自动拉起 Chrome 调试模式也偏向 macOS

如果远程调试端口没开，脚本会在 macOS 下调用：

```bash
open -na "Google Chrome" --args --remote-debugging-port=9222 ...
```

其他平台目前没有同等自动启动逻辑。

### 9.4 Python 运行环境是隐式依赖

Node 脚本直接调用 `python3 scripts/workbook_helper.py`，并要求环境里有 `openpyxl`。

但仓库里目前没有看到：

- `requirements.txt`
- `pyproject.toml`

这意味着 Python 依赖现在更像“环境前提”，而不是被项目显式声明和自动安装的依赖。

## 10. 一些容易忽略的真实细节

### 10.1 当前实现并没有直接操作 WPS

仓库名叫 `chatgpt-wps-automation`，配置里也还有 `wps` 字段，`scripts/applescript/` 下也保留了一组 AppleScript：

- `copy_range.applescript`
- `paste_column.applescript`
- `goto_cell_via_name_box.applescript`
- `get_frontmost_app.applescript`

但从当前主入口代码看，这些脚本没有被 `ui-server.mjs` 或 `chatgpt-wps-loop.mjs` 调用。

也就是说：

- 它们更像早期方案或备用资产。
- 当前有效主流程已经转向“文件级读写 Excel”，而不是“模拟键盘去驱动 WPS”。

### 10.2 配置文件里还有未被当前默认配置覆盖的字段

当前仓库里的 `automation.config.json` 含有 `wps` 配置块，但默认配置对象里没有对应字段。

这说明：

- 配置文件可能经历过版本演进。
- 部分字段现在只是兼容性遗留，不一定参与运行逻辑。

### 10.3 项目是“强依赖 ChatGPT 页面结构”的

很多关键动作都靠页面选择器完成，比如：

- 输入框定位
- assistant 消息定位
- Stop 按钮检测
- New chat 按钮点击

这类方案优点是实现快、成本低、能复用 ChatGPT project。

缺点是：

- 一旦 ChatGPT 网页结构明显变化，脚本可能失效。
- 多语言页面、A/B 测试或 UI 改版都可能影响稳定性。

## 11. 可以怎么理解这个项目的设计取舍

如果从工程取舍看，这个项目选择的是一条很务实的路线：

- 放弃官方 API 接入的规范性。
- 换取直接复用 ChatGPT project 和人工维护的提示词环境。
- 放弃对桌面表格软件的 UI 自动化。
- 换取更稳定的文件级 Excel 读写。
- 用极简原生 UI。
- 换取部署和理解成本低。

因此它更像一个“个人/小团队可快速落地的自动化工具”，而不是一个抽象程度很高的平台型系统。

## 12. 总结

一句话概括，这个项目的原理就是：

“用本地 Node 服务承接配置和任务控制，用 Playwright 连接真实 ChatGPT 网页完成批量生成，再用 Python `openpyxl` 将结果批量写回 Excel。”

如果再拆成一句更偏实现层的话，就是：

“配置文件驱动 + 浏览器自动化 + 批处理循环 + 二维矩阵回写。”

这也是它当前最关键的几个技术支点：

- `automation.config.json` 负责持久化任务参数和 ChatGPT project 绑定信息。
- `chatgpt-wps-loop.mjs` 负责浏览器连接、循环调度、回复判定和结果解析。
- `workbook_helper.py` 负责稳定地读取和回写工作簿。
- `ui-server.mjs` 和 `ui/index.html` 负责把这些能力包装成可操作的本地控制台。
